import { ScriptComposer } from 'script-composer-sdk';

async function main() {
  try {
    // 创建 ScriptComposer 实例
    const composer = new ScriptComposer();
    
    console.log('Script Composer SDK 示例');
    console.log('------------------------');
    
    // 这里可以添加更多 SDK 使用示例
    // 例如：创建脚本、执行交易等
    
  } catch (error) {
    console.error('Error:', error);
  }
}

main(); 